[build]
  command = "npm run build"
  publish = "dist"

# SPA (React) sayt uchun: barcha manzillarni index.html ga yo'naltiradi.
# Shu tufayli /admin manzili ham ochiladi.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
